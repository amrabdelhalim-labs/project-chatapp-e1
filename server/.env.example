PORT=5000
hostname=localhost
MONGODB_URL=mongodb://127.0.0.1:27017/mychat
JWT_SECRET=your_jwt_secret_here

# ─── Storage Configuration ────────────────────────────────────────────────────
# Options: local | cloudinary | s3  (default: local)
STORAGE_TYPE=local

# Local
# Makes local storage return absolute URLs in API responses.
# Without SERVER_URL, the server returns relative paths (/uploads/...),
# and the mobile app's normalizeImageUrl() reconstructs the full URL.
# SERVER_URL=http://192.168.1.112:5000

# Custom uploads directory path and URL prefix (optional, both have sensible defaults)
# LOCAL_UPLOADS_DIR=/custom/path/to/uploads
# LOCAL_BASE_URL=/uploads

# For cloud providers: pre-upload a default profile picture and paste its URL here.
# LOCAL fallback: default-picture.jpg served by express.static is used automatically.
# Run: npm run check-default-picture (checks and uploads for cloudinary/s3, verifies for local)
# DEFAULT_PROFILE_PICTURE_URL=https://res.cloudinary.com/your-cloud/image/upload/.../default-picture.jpg

# ─── Cloudinary (STORAGE_TYPE=cloudinary) ────────────────────────────────────
# Option A: Heroku addon (auto-set by heroku addons:create cloudinary)
#   Format: cloudinary://API_KEY:API_SECRET@CLOUD_NAME
# CLOUDINARY_URL=cloudinary://your_api_key:your_api_secret@your_cloud_name
#
# Option B: Manual (individual vars — used if CLOUDINARY_URL is not set)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
# CLOUDINARY_FOLDER=mychat-profiles

# ─── AWS S3 (STORAGE_TYPE=s3) ─────────────────────────────────────────────────
# Bucket must allow public read via a bucket policy (not ACL — legacy ACLs are
# blocked by default on new AWS accounts). Alternatively, front with CloudFront.
# AWS_S3_BUCKET=
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_FOLDER=uploads/profiles
# AWS_S3_FOLDER=uploads/profiles
